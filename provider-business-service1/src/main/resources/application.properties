# 建议小写
spring.application.name=provider-business-service

server.port=7901
#user.userServicePath=http://127.0.0.1:7900/sample/
# 服务注册
eureka.client.serviceUrl.defaultZone=http://admin:666666@localhost:8761/eureka
eureka.instance.preferIpAddress=true
eureka.instance.instanceId=${spring.application.name}:${spring.application.instanceId:${server.port}}
# 访问验证
spring.security.user.name=admin
spring.security.user.password=111111